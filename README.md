# Landing Page (Recruiter)

Static landing page for a QA Engineer / SDET profile.

## Local preview

- Option 1 (Python):

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

- Option 2 (Node):

```bash
npx serve .
```
